
# 🧪 Firecircle Signaling Server — Example Environment

# Host and port where the signaling server listens
SIGNAL_HOST=0.0.0.0
SIGNAL_PORT=8787

# Allowed origins for WebSocket connections (comma-separated)
# Local development setup (Vite dev server, Next.js, etc.)
CORS_ORIGIN=http://localhost:5173

# To allow multiple local origins or wildcards:
# CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
# CORS_ORIGIN=*

# 🧩 Example usage:
#   pnpm dev:server
#   pnpm start:prod
#
# Make sure your frontend (e.g., Playground) uses ws://localhost:8787 as SIGNAL_URL