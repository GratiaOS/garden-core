---
"@gratiaos/ui": patch
---

Fix Select primitive to expose `variant` via `data-variant` for skins (e.g. ghost) and tighten type handling in demos.

whisper: choices rest easier when the skin knows their shape. 🌬️
