/**
 * Experimental placeholder for the GratiaOS kernel.
 * Only example scene tapes are shipped today under /examples.
 *
 * A future release will expose the actual heart-state runtime and helpers.
 */
export const KERNEL_EXPERIMENTAL = true;
